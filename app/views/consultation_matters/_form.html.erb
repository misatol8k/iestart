<%= form_with(model: @consultation_matter, local: true) do |form| %>
  <%# if consultation_matter.errors.any? %>
    <!-- <div id="error_explanation">
      <h2><%#= pluralize(consultation_matter.errors.count, "error") %> prohibited this consultation_matter from being saved:</h2>

      <ul>
      <%# consultation_matter.errors.full_messages.each do |message| %>
        <li><%#= message %></li>
      <%# end %>
      </ul>
    </div> -->
  <%# end %>

  <div class="field">
    <h4>相談事項</h4>
    <%= form.collection_check_boxes(:consultation_matter_ids, ConsultationMatter.all, :id, :item) do |consultation_matter| %>
      <%= consultation_matter.label do %>
        <%= consultation_matter.check_box %>
        <%#= consultation_matter.item %>

        <%= render partial: "consultation_details/form" %>
      <% end %>
    <% end %>
  </div>

<% end %>
